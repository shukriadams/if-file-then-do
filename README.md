# if-file-then-do

Run shell commands based on file changes

## Setup

    npm install
    jspm install -y
    npm run client

## Settings

    port: 3000
    watch:
        - path: c:/
          type: new
          do:
             